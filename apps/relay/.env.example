# Copy the repo root .env.example instead; this file exists only so
# apps/relay has its own .env when run standalone outside the monorepo dev
# script. Same variable names, same meaning.
SARVAM_API_KEY=
SARVAM_SPEAKER=simran
SARVAM_LANGUAGE=auto
STT_MODE=ws
TTS_MODE=rest
WEB_ORIGIN=http://localhost:3000
RELAY_SHARED_SECRET=
RELAY_PORT=8787
